server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/odtufatih/spring-cloud-config-repo
          searchPaths: foa
          default-label: master